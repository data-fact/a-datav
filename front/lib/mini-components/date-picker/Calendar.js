import dayjsGenerateConfig from 'rc-picker/lib/generate/dayjs';
import generateCalendar from 'antd4/lib/calendar/generateCalendar';

const Calendar = generateCalendar(dayjsGenerateConfig);

export default Calendar;
